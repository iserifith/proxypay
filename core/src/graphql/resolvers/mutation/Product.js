import { Product } from "@models/Product.model";

export default {
  // createProduct: async (parent, args, context, info) => {
  //   const { title, price, attachments, isPublicListed, isActive } = args;
  //   const newProduct = await Product.create({
  //     title,
  //     price,
  //     attachments,
  //     isPublicListed,
  //     isActive
  //   });
  // }
};
